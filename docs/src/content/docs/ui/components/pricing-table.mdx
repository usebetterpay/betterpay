---
title: Pricing Table
description: Opinionated pricing section built on PlanGroup and PlanCard.
---

`PricingTable` is a thin section wrapper: title, description, interval toggle, and plan grid.

<ComponentPreview
  fullWidth
  code={`import { PricingTable } from '@betterpay/ui';

<PricingTable
  plans={plans}
  onSelectPlan={(planId, interval) => {}}
/>`}
>
  <DemoPricingTable />
</ComponentPreview>

## Installation

```bash title="Terminal"
pnpm add @betterpay/ui
```

```tsx title="Usage"
import { PricingTable, type PlanView } from '@betterpay/ui';
```

## Examples

### Compact density

```tsx
<PricingTable plans={plans} density="compact" onSelectPlan={...} />
```

### Controlled interval

```tsx
<PricingTable
  plans={plans}
  interval={interval}
  onIntervalChange={setInterval}
  onSelectPlan={handleSelect}
/>
```

## API Reference

### PricingTable

| Prop | Type | Default |
| --- | --- | --- |
| `plans` | `PlanView[]` | — |
| `title` | `string` | `"Choose a plan"` |
| `description` | `string` | IDR-oriented default copy |
| `interval` | `"month" \| "year"` | — |
| `defaultInterval` | `"month" \| "year"` | `"month"` |
| `onIntervalChange` | `(interval) => void` | — |
| `onSelectPlan` | `(planId, interval) => void` | — |
| `currencyFallback` | `string` | `"IDR"` |
| `density` | `"comfortable" \| "compact"` | `"comfortable"` |

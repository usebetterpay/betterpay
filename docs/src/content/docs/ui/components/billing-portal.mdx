---
title: Billing Portal
description: Composed billing page — summary, plan actions, usage, and invoices.
---

`BillingPortal` is a presentational layout. Interact with plan switcher, cancel, and usage expand in the preview.

<ComponentPreview
  fullWidth
  code={`import { BillingPortal } from '@betterpay/ui';

<BillingPortal
  subscription={subscription}
  plans={plans}
  entitlements={entitlements}
  invoices={invoices}
  onChangePlan={...}
  onCancel={...}
/>`}
>
  <DemoBillingPortal />
</ComponentPreview>

## Installation

```bash
pnpm add @betterpay/ui
```

## API Reference

### BillingPortal

| Prop | Type | Default |
| --- | --- | --- |
| `subscription` | `SubscriptionView` | — |
| `plans` | `PlanView[]` | `[]` |
| `entitlements` | `EntitlementView[]` | `[]` |
| `invoices` | `InvoiceView[]` | `[]` |
| `callout` | `{ status, title?, description? } \| null` | `null` |
| `onChangePlan` | `(planId: string) => void` | — |
| `onCancel` | `(payload: { reasonId?: string }) => void` | — |
| `onDownloadInvoice` | `(invoiceId: string) => void` | — |
| `invoiceLayout` | `"table" \| "cards" \| "responsive"` | `"responsive"` |
| `usagePeriodLabel` | `string` | — |
| `title` | `string` | `"Billing"` |

import {describe,expect,it} from 'vitest'; import {generateKeyPairSync,createVerify} from 'node:crypto'; import {createSignature,sha256Hex} from '../src/signature'; const {privateKey,publicKey}=generateKeyPairSync('rsa',{modulusLength:2048}); const priv=privateKey.export({type:'pkcs8',format:'pem'}).toString(); const pub=publicKey.export({type:'spki',format:'pem'}).toString(); describe('Cashlez RSA',()=>{ it('rsa sign',()=>{const body=JSON.stringify({orderId:'order-1'}); const sig=createSignature('POST','/generate_url_vendor',body,'2024-01-01T00:00:00Z',priv); const d=sha256Hex(body); const s=`POST:/generate_url_vendor:${d}:2024-01-01T00:00:00Z`; const v=createVerify('RSA-SHA256'); v.update(s,'utf8'); v.end(); expect(v.verify(pub, Buffer.from(sig,'base64'))).toBe(true);}); });
